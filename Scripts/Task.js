export default class Task {
    constructor(name,sum,term,starterSum) {
        this.name           = name;
        this.sum            = sum;
        this.term           = term;
        this.starterSum     = starterSum;
    }
}